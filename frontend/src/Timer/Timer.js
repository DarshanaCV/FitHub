import SetTimer from "./SetTimer"
const Timer=()=>{
    return(
        <div className="timer-container">
            <h1>Timer</h1>
            <SetTimer/>
        </div>
    )
}
export default Timer